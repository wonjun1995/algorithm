#include<bits/stdc++.h>
using namespace std;
//부서진 키보드가 있음.
//부서진 키보드는 공백을 입력하기 위한 스페이스바와 스페이스바 외의 다른 n개의 키만 사용이 가능
//n이 4고 l, i, n, e 4개의 키를 사용할 수 있다고 했을 때, "line in line", "in    n          in"과 같은 문장을 만들 수 있습니다.
//대문자를 만들려면 shift키와 해당 알파벳의 소문자 키가 필요
//"L I N e"을 완성하려면 shift, l, i, n, e 이렇게 5개의 키가 필요
//"coding is fun"이라는 문자열을 완성하면 13점을 얻을 수 있습니다.
//단, 완성된 문자열 속에 알파벳 대문자가 있을 경우 알파벳 대문자 개수만큼 1점씩 추가로 점수를 얻을 수 있습니다.

int solution(vector<string> sentences, int n) {
    int answer = -1;
    return answer;
}


int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    return 0;
}

/* 

["line in line", "LINE", "in lion"],5	
20
["ABcD", "bdbc", "a", "Line neWs"],7	
12
 */